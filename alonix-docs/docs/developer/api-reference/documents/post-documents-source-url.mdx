---
title: "POST /documents/source-url"
description: "Resolve presigned source URL for citation"
sidebar_position: 13
custom_edit_url: null
---

import PlaygroundLink from '@site/src/components/PlaygroundLink';

# POST /documents/source-url

**[Try in API Playground](/api-playground?path=%2Fdocuments%2Fsource-url&method=post)**

| | |
|---|---|
| **Method** | `POST` |
| **Path** | `/api/documents/source-url` |
| **Tag** | Documents |
| **Auth** | Bearer JWT |

## Request body

**Content-Type:** `application/json`

```json
{
  "fileKey": "string",
  "documentId": "string"
}
```

## Responses

| Status | Description |
|--------|-------------|
| 200 | Presigned URL |

## Code examples

### cURL

```bash
curl -X POST "http://localhost:4010/documents/source-url" \
  -H "Content-Type: application/json" \
  -d '{
  "fileKey": "string",
  "documentId": "string"
}'
```

### JavaScript (fetch)

```javascript
const response = await fetch('http://localhost:4010/documents/source-url', {
  method: 'POST',
  headers: {
    'Authorization: Bearer <token>',
    'Content-Type: application/json',
  },
  body: JSON.stringify({
      "fileKey": "string",
      "documentId": "string"
  }),
});

const data = await response.json();
if (!response.ok) throw new Error(data.message || response.statusText);
console.log(data);
```

<PlaygroundLink path="/documents/source-url" method="post" />

## Related

- [API overview](/developer/api-reference)
- [Authentication](/developer/authentication)
- [Error handling](/developer/error-handling)
