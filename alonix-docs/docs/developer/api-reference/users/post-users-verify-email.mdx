---
title: "POST /users/verify-email"
description: "Verify email with OTP"
sidebar_position: 18
custom_edit_url: null
---

import PlaygroundLink from '@site/src/components/PlaygroundLink';

# POST /users/verify-email

**[Try in API Playground](/api-playground?path=%2Fusers%2Fverify-email&method=post)**

| | |
|---|---|
| **Method** | `POST` |
| **Path** | `/api/users/verify-email` |
| **Tag** | Users |
| **Auth** | None |

## Request body

**Content-Type:** `application/json`

```json
{
  "email": "user@example.com",
  "otp": "string"
}
```

## Responses

| Status | Description |
|--------|-------------|
| 200 | Email verified |
| 400 |  |

## Code examples

### cURL

```bash
curl -X POST "http://localhost:4010/users/verify-email" \
  -H "Content-Type: application/json" \
  -d '{
  "email": "user@example.com",
  "otp": "string"
}'
```

### JavaScript (fetch)

```javascript
const response = await fetch('http://localhost:4010/users/verify-email', {
  method: 'POST',
  headers: {
    'Content-Type: application/json',
  },
  body: JSON.stringify({
      "email": "user@example.com",
      "otp": "string"
  }),
});

const data = await response.json();
if (!response.ok) throw new Error(data.message || response.statusText);
console.log(data);
```

<PlaygroundLink path="/users/verify-email" method="post" />

## Related

- [API overview](/developer/api-reference)
- [Authentication](/developer/authentication)
- [Error handling](/developer/error-handling)
